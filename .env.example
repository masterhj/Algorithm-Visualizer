# Copy to .env and fill in. The app runs fine without MySQL — it just stops
# keeping a history of runs.

PORT=3001

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=algorithm_visualizer
